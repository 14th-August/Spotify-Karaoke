// Stub component — not currently mounted. Reserved for the top-of-app
// nav bar (logo, profile menu, etc.) once the app has more than one page.

export default function Header() {
    return (
        <header>
            <h1>Hello, Vite!</h1>
        </header>
    )
}